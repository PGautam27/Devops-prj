apiVersion: v1
kind: Service
metadata:
  name: bookserver
  namespace: default
spec:
  type: NodePort
  selector:
    app: bookserver
  ports:
    - port: 3000          
      targetPort: 3000    
      nodePort: 30001     
